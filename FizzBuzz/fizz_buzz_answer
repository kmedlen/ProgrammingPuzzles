first  = input('Enter 1st number: ')
second = input('Enter second number: ')

for count in range(first, second + 1):
# count = range(first, second + 1)
        print "FizzBuzz" if count % 3 == 0 and count % 5 == 0 else "Fizz" if count % 3 == 0 else "Buzz" if count % 5 == 0 else count

# Once I got rid of my syntax errors (I had multiple 'prints' and was using parenthesis incorrectly), I got the code to run
# however, it only prints FizzBuzz so it must not be iterating through all of the logic correctly.
 
# got it.  
